Install the following into a virtual enviroment venv

chess
pygame


You should only touch start.py and config.py. Start the game with python start.py

Interact with the chess game using start.py. You can extract the PGN from game.py -> game.PGN (newGame.pgn) It is updated everytime a new move is made. I 

Use config.py to set display parameters